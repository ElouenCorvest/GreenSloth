function mapClick(evt) {
    console.log("hello")
}

// import Swiper from 'swiper'

var swiper = new Swiper('.swiper-container', {

    direction: 'vertical',
    mousewheel: true,
    slidesPerView: 1,
    grabCursor: true,
    loop: true,
    // freeMode: true,
    // freeModeSticky: true
  });